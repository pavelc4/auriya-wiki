# Auriya Wiki

Technical documentation built with Docusaurus and Bun.

```bash
bun install
bun start
```

Validate the production output with:

```bash
bun run typecheck
bun run build
```

The generated static site is written to `build/`. Deployment configuration is intentionally not included yet.
